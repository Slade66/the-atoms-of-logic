---
created: 2026-03-28T13:29:41+08:00
---

# `tee` 命令

- **作用：** `tee` 命令像水管三通，把输入的数据同时输出到终端 + 文件。

- **优点：**
    1. **一行命令搞定写文件：** 多行内容直接写文件，不用 `vim`。
    2. **实时预览：** 边写边看内容，确认无误。

- **逻辑图：**

    ```
    输入 → tee → 终端显示
            ↓
        写入文件
    ```
