---
description: 将 atoms 目录下的原始笔记结构化，合并重复表达
---

## 目标

合并相近章节 + 去重复表达。保持原文标题层级风格，不强行改变。

## 步骤

1. 读取用户指定的 `.md` 文件。

2. 识别并合并：
   - **去重复** 同一意思出现多次的，只保留最清晰的一处，其余删除。
   - **合并章节** 语义相近的多个章节合并为一个章节，标题取最概括的那个。

3. 用 edit 工具直接修改原文件，不要创建新文件。

4. 完成后输出：
   - 合并后的骨架（标题列表）
   - 合并了哪些重复 / 哪些章节被合并
