---
created: 2026-06-08T11:48:19+08:00
---

# AI 会写很多冗余的代码

// 2. 按 origin_plan_uuid 读各子表。
	var (
		specs    []model.PlanDeviceSpec
		devices  []model.PlanDevice
		conns    []model.PlanConnection
		ics      []model.PlanInterconnect
		routings []model.PlanRouting
		roleAs   []model.PlanRoutingRoleAs
		segs     []model.PlanIPSegment
		bips     []model.PlanBusinessIP
		tmpls    []model.PlanDeviceTemplate
	)
	for _, q := range []struct {
		dst any
	}{
		{&specs}, {&devices}, {&conns}, {&ics}, {&routings}, {&roleAs}, {&segs}, {&bips}, {&tmpls},
	} {
		if err := db.Where("origin_plan_uuid = ?", uuid).Find(q.dst).Error; err != nil {
			return nil, err
		}
	}

ai 会写很多晦涩难懂的代码，基本很难被同事看懂、维护

总是自顶向下设计写代码。
