---
created: 2026-05-01T08:38:24+08:00
---

# Appsmith 如何把字段显示成下拉选择

### 一、问题背景

- **使用场景** 在 Appsmith 中展示借阅状态时，不希望用户直接输入 `PENDING`、`BORROWED` 这类状态值，而是希望通过下拉选择来展示和选择状态。

- **核心做法** 将字段的 `Field Type` 设置为 `Select`，再在 `Options` 中配置每个状态对应的显示文本和实际值。

### 二、配置字段类型

1. 找到需要展示状态的字段配置。

2. 将 `Field Type` 设置为 `Select`。

3. 进入 `Options` 配置项，填入状态选项数组。

### 三、配置静态 Options

- **配置格式** `Options` 需要使用数组，每一项都包含 `label` 和 `value`。

- **显示文本** `label` 是页面上展示给用户看的中文状态。

- **实际值** `value` 是真正提交或保存到数据库里的状态枚举值。

    ```json
    {{
      [
        { "label": "待审核", "value": "PENDING" },
        { "label": "借阅中", "value": "BORROWED" },
        { "label": "已归还", "value": "RETURNED" },
        { "label": "已拒绝", "value": "REJECTED" },
        { "label": "已取消", "value": "CANCELLED" }
      ]
    }}
    ```

### 四、配置动态 Options

- **适用场景** 如果下拉选项来自数据库表，而不是固定枚举值，就需要通过查询动态生成 `Options`。

- **查询数据** 新建查询 `GetCultureCategories`，从分类表中取出 `id` 和 `name`。

    ```sql
    SELECT id, name
    FROM tea_culture_category
    ORDER BY sort_order ASC, id ASC;
    ```

- **加载时机** 将查询设置为 `Run on page load`，确保页面打开后先加载下拉选项数据。

- **选项映射** 在 `Options` 中把查询结果映射成 Appsmith Select 需要的 `label` / `value` 格式。

    ```js
    {{ GetCultureCategories.data.map(c => ({
      label: c.name,
      value: c.id
    })) }}
    ```

- **默认选中** 在 `Default selected value` 中填入当前表单数据里的分类 ID，让编辑场景能自动回显原值。

    ```js
    {{ update_form.sourceData.category_id }}
    ```

### 五、理解 label 和 value 的区别

- **label 面向用户** `label` 负责让页面更易读，例如显示“待审核”“借阅中”。

- **value 面向系统** `value` 负责和后端或数据库保持一致，例如保存 `PENDING`、`BORROWED`。

- **通用原则** 页面展示可以用中文，但数据传递尽量使用系统稳定识别的枚举值。
