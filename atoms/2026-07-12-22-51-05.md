---
created: 2026-07-12T22:51:09+08:00
---

# 资源的单复数命名规范

## 一、Go 包名使用单数

- **命名规则：** Go 的 package 名通常使用简短的单数形式，因此任务模块命名为 `task`，目录使用 `internal/task/`。

## 二、集合资源使用复数

- **适用场景：** `tasks` 表示任务集合，适合用于 HTTP 资源路径和数据库表名。

    - HTTP 路径：`GET /api/tasks`。

    - 数据库表：`tasks`。
