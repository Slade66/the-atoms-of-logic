---
created: 2026-04-17T18:17:00+08:00
---

# SQL 检查列是否存在

- **用途：** 查询某张表里是否存在指定列，常用于写幂等的 DDL 迁移脚本。

- **语法：**

    ```sql
    SHOW COLUMNS FROM `order_main` LIKE 'points_deduct';
    ```

- **返回结果：** 如果列存在，返回该列的元信息（类型、是否可空、默认值等）；如果不存在，返回空结果集。
