---
created: 2026-05-20T03:12:12+08:00
---

# 升级 npm 包

## 一、升级 npm 自身

- **升级原理** `npm` 本身也是一个包，因此可以使用 `npm` 来更新 `npm` 自己。

    ```bash
    npm install -g npm@latest
    ```

## 二、升级全局包

- **升级命令** 将指定的全局包升级到最新版。

    ```bash
    npm install -g package-name@latest
    ```
