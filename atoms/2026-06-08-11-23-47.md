---
created: 2026-06-08T11:23:49+08:00
---

# 用伪代码解释逻辑，这样能检查到逻辑问题

按 设备×VRF 出行，还原 v1 形态）

  function vpnPlanRows():
      rows = []
      # 改成遍历设备，用设备身上的 VRFUUID 反查 VRF 规格
      for d in plan.Devices:
          if d.VRFUUID == "":               # 该设备没挂 VRF，跳过
              continue
          v = vrfByUUID[d.VRFUUID]           # 取这台设备所属 VRF 的 RD/RT
          if v not found:                    # 规格缺失，跳过（或出空 RD 行，按需）
              continue
          rows.append({
              DeviceName: d.DeviceName       # ← 关键：把设备名填回来
              VRF:   v.Name
              RD:    v.RD
              InRT:  v.InRT
              OutRT: v.OutRT
          })
      # 排序保证导出可复现：先按设备名，再按 VRF
      sort rows by (DeviceName, VRF)
      return rows

  结果：每台设备一行（截图里的 8 行），设备名填好。
